
flexbox-layout(main, cross, direction)
    display: flex
    justify-content: main
    align-items: cross
    flex-direction: direction


flexbox-layout-row(main=flex-start, cross=center)
    flexbox-layout(main, cross, row)
